<%= form_with(model: user, local: true) do |f| %>
  <div class="field">
    <div class="control">
      <%= f.label :username, class: "label" %>
      <%= f.text_field :username, class: "input", autofocus: true %>
    </div>
  </div>

  <div class="field">
    <div class="control">
      <%= f.submit "Sign Up", class: "button is-primary is-large is-fullwidth" %>
    </div>
  </div>
<% end %>
